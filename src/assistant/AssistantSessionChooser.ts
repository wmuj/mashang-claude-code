// Restored-tree fallback component: keeps import path valid without forcing
// an incomplete UI implementation.
export const AssistantSessionChooser: (
  props: Record<string, unknown>,
) => null = () => null;
